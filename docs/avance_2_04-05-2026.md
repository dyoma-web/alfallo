# Avance 2 - 2026-05-04

Segundo corte de continuidad del trabajo en `alfallo`, después del primer documento `avance_04-05-2026.md`.

Contexto: seguimos trasladando criterio visual/UX desde `alfallo-gpt`, pero manteniendo aquí la versión funcional conectada a GitHub y preparada para Sheets/Apps Script. La prioridad fue no romper lo operativo.

---

## Estado general

La rama `main` está sincronizada con GitHub. Los últimos bloques implementados y pusheados fueron:

- `acdd70c` - Categorías jerárquicas de sede.
- `c9c1ddb` - Bloqueos de sede aplicados a calendario y booking.
- `c587a76` - Permisos cruzados de profesionales por sede compartida.
- `9052f88` - Booking respeta franja de trabajo del profesional.
- `8be0778` - Gestion completa de bloqueos de sede.
- `8412304` - Editor visual de franja profesional.

Verificaciones ejecutadas durante los bloques:

- `npm run typecheck`
- `npm run gs:bundle`
- `npm run build`
- `git diff --check`

---

## Avances cerrados

### 1. #8 - Categorías jerárquicas de sede

Primer corte funcional sobre Sheets.

Se agregó soporte para:

- `categoria_sede`
- `categoria_rank`

Archivos clave:

- `apps-script/schema.gs`
- `apps-script/admin.gs`
- `apps-script/options.gs`
- `apps-script/bookings.gs`
- `src/components/admin/SedeFormModal.tsx`
- `src/pages/AdminSedes.tsx`
- `src/pages/Booking.tsx`
- `src/components/calendar/CalendarView.tsx`

Resultado:

- Admin puede definir categoría de sede: Básica, Plus, Premium, Elite.
- AdminSedes muestra la categoría como badge y en detalle.
- Booking recibe categoría/rango/base de la sede.
- Booking advierte si el usuario elige una sede distinta a su base o superior en categoría.

Estado: cerrado como primer corte.

Pendiente de decisión: si una sede superior debe bloquear, requerir autorización o generar cobro diferencial.

---

### 2. #12 - Bloqueos de sede aplicados

Se conectó la hoja `sedes_bloqueos` al flujo real.

Archivos clave:

- `apps-script/availability.gs`
- `apps-script/bookings.gs`
- `apps-script/Code.gs`
- `src/pages/AdminCalendar.tsx`
- `src/components/admin/SedeBlockFormModal.tsx`
- `src/components/calendar/CalendarView.tsx`
- `src/pages/Booking.tsx`

Resultado:

- Admin puede crear bloqueos de sede desde calendario global.
- Los bloqueos se pintan como eventos de fondo en el calendario.
- `submitBooking` rechaza reservas dentro de una franja de sede bloqueada.
- Booking consulta el estado del slot y deshabilita el submit si la sede aparece bloqueada.

Estado: cerrado como corte funcional ampliado.

Actualización posterior:

- AdminCalendar lista los bloqueos activos del rango visible.
- El modal permite editar sede, motivo y rango horario.
- El modal permite eliminar bloqueos existentes.

---

### 3. #10 - Permisos cruzados profesionales

Primer corte MVP sin introducir `ProfessionalPresence`.

Archivos clave:

- `apps-script/trainer.gs`
- `src/pages/TrainerUsers.tsx`
- `src/pages/UserDetailForTrainer.tsx`

Resultado:

- Un profesional puede ver clientes asignados directamente.
- También puede ver clientes compartidos por sede activa usando:
  - `sedes_entrenadores`
  - `sedes_usuarios`
- `/usuarios` muestra badge de "Sede compartida".
- El perfil operativo del cliente indica la sede compartida.
- La selección de miembros en grupos se beneficia porque usa `listMyUsers`.

Estado: cerrado como primer corte.

Pendiente: granularidad por acción si se requiere distinguir entre ver, agendar, registrar asistencia, editar grupos, etc.

---

### 4. #11 - Booking respeta presencia/franja profesional

Primer corte usando `franja_trabajo` existente en `entrenadores_perfil`.

Archivos clave:

- `apps-script/bookings.gs`
- `src/pages/Booking.tsx`
- `src/components/admin/TrainerProfileModal.tsx`

Resultado:

- Si el profesional tiene `franja_trabajo` configurada, `submitBooking` bloquea reservas fuera de esa franja.
- `getSlotCapacity` informa `trainerFueraHorario`.
- Booking muestra aviso antes de enviar.
- El botón de agendar se deshabilita si el slot cae fuera de la franja de atención.
- El admin puede editar visualmente la semana de trabajo del profesional con uno o varios rangos por dia.

Estado: cerrado como corte funcional ampliado.

---

### 5. Sedes de cliente administrables

Se conectó `sedes_usuarios` a una interfaz administrativa directa.

Archivos clave:

- `apps-script/admin.gs`
- `apps-script/Code.gs`
- `src/pages/AdminUsers.tsx`

Resultado:

- Admin puede abrir la gestión de sedes desde Usuarios para perfiles cliente.
- Se pueden asignar varias sedes y marcar una como sede base.
- El listado de usuarios refleja la sede base en los badges.
- El backend reemplaza las relaciones de `sedes_usuarios` de forma controlada y deja auditoría.

Estado: cerrado como primer corte.

---

### 6. UX fina de Booking

Se agrego consulta por lote para pintar el estado operativo de los horarios visibles.

Archivos clave:

- `apps-script/bookings.gs`
- `apps-script/Code.gs`
- `src/pages/Booking.tsx`

Resultado:

- Booking consulta estados de slots visibles con `getSlotStates`.
- La grilla deshabilita horas bloqueadas por sede, fuera de franja profesional, no disponibles o llenas con cupo estricto.
- El submit tambien queda protegido si el slot seleccionado se vuelve invalido.
- `getSlotCapacity` ahora informa `trainerNoDisponible`.

Estado: cerrado como primer corte.

---

### 7. Planes globales y personalizados por profesional

Se ajusto el catalogo de planes para soportar alcance global/admin y planes personalizados de profesionales.

Archivos clave:

- `apps-script/schema.gs`
- `apps-script/admin.gs`
- `src/pages/AdminPlanes.tsx`
- `src/components/admin/PlanFormModal.tsx`
- `src/routes.tsx`

Resultado:

- Admin y profesionales pueden acceder a `/planes`.
- Los planes admin quedan como planes globales ligados a gimnasio, area profesional, categoria profesional y categoria de sede.
- Los profesionales pueden crear planes personalizados; el backend los marca con `owner_id` y solo son visibles para su creador, ademas del admin.
- Se agrego clasificacion amplia: area de entrenamiento, area medica y otra area.
- Se agrego clasificacion especifica: entrenamiento personalizado, clases grupales, nutricion, fisioterapia, evaluacion medica u otra categoria.
- Los planes ya no dependen de un entrenador titular como asignacion administrativa; la propiedad del profesional es tecnica.
- La actualizacion de precio distingue `future_only` para compras posteriores y `global` para actualizar planes activos existentes.
- Los planes de usuario conservan snapshot de precio en `precio_pagado`, salvo cuando se elige actualizacion global.
- Se reemplazo la vinculacion operativa a sede especifica por categoria de sede (`basica`, `plus`, `premium`, `elite`).
- Profesionales pueden sugerir cambios a planes globales; la solicitud llega al modulo de Solicitudes para revision del admin.

Estado: cerrado como primer corte.

---

## Pendientes priorizados

| Pendiente | Carga | Prioridad | Comentario |
|---|---:|---:|---|
| Definir política de sedes superiores (#8) | S | Alta | Decidir si bloquea, requiere autorización o cobra diferencial. Hoy solo advierte. |
| QA end-to-end operativo | M | Alta | Probar flujo completo admin/trainer/cliente con Sheets real. |
| Granularidad fina de permisos cruzados | M/L | Media | Hoy el permiso compartido es por sede; falta decidir permisos por acción. |
| Alertas avanzadas | M | Media | Baja asistencia, plan por vencer, conflictos operativos automáticos. |
| ProfessionalPresence real | XL | Futuro | No conviene forzarlo sobre Sheets. Mejor reservarlo para Postgres/capa robusta. |
| Deploy Apps Script + bootstrap | S/M | Alta | Pegar bundle y ejecutar `bootstrap()` cuando haya columnas nuevas. |

---

## Recomendación de siguiente paso

Siguiente bloque más útil: **QA end-to-end operativo**.

Razón: ya se cerraron varios bloques funcionales conectados entre admin, trainer, cliente, Apps Script y Sheets. Antes de ampliar alertas o granularidad fina, conviene validar el circuito completo con datos reales. El alcance razonable sería:

1. Crear/editar sede, categoria, bloqueo y asignacion de cliente.
2. Configurar perfil profesional, franja de trabajo y sedes compartidas.
3. Agendar desde cliente validando cupos, sede base, bloqueos y franja profesional.
4. Revisar calendario/admin/trainer y auditoria.

Después de eso, el segundo bloque más rentable sería **alertas avanzadas**.

---

## Nota operativa

Cada vez que se regenere `apps-script/bundle/Code.gs`, hay que pegar el bundle actualizado en Apps Script. Para cambios de columnas en Sheets, ejecutar `bootstrap()` para agregar headers faltantes sin recrear el documento.
